# DigitalBoda Backend Environment Configuration
# Copy this file to .env.staging or .env.production and update the values

# Django Configuration
SECRET_KEY=django-insecure-your-secret-key-here-change-this
DEBUG=False
DJANGO_ENV=production

# Database Configuration
DB_NAME=digitalboda_db
DB_USER=digitalboda_user
DB_PASSWORD=your_database_password_here
DB_HOST=localhost
DB_PORT=5432

# Allowed Hosts (comma-separated)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,localhost,127.0.0.1

# CORS Settings (comma-separated origins for production)
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Security Settings
SECURE_SSL_REDIRECT=True

# ID Encryption (IMPORTANT: Use scripts/generate_encryption_key.py to generate these)
# ID_ENCRYPTION_KEY must be a base64-encoded Fernet key (44 characters)
# Example: gAAAAABh... (generated by Fernet.generate_key())
ID_ENCRYPTION_KEY=REPLACE_WITH_GENERATED_FERNET_KEY
ID_HASH_SALT=REPLACE_WITH_GENERATED_SALT

# Media/Static Files
STATIC_URL=/static/
MEDIA_URL=/media/

# Firebase Configuration
FIREBASE_CREDENTIALS_PATH=path/to/firebase-credentials.json

# Redis Configuration (for Celery)
REDIS_URL=redis://localhost:6379/0

# Email Configuration (if needed)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@domain.com
EMAIL_HOST_PASSWORD=your_email_password

# Logging Level
LOG_LEVEL=INFO
